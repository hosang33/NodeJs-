<!DOCTYPE html>
<html>
<head>
    <meta charset='utf-8'>
    <meta http-equiv='content-type' content='text/html; charset=UTF-8'>
    <title>Page Title</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.4.1.js"integrity="sha256-WpOohJOqMqqyKL9FccASB9O0KwACQJpFTUBLTYOVvVU=" crossorigin="anonymous"></script>
</head>
<body>
    <div class="container">
        <div class="row" style="margin-bottom: 150px;"></div>
        <div class="row">
            <div class="col-sm-4"></div>
            <div class="col-sm-4">
                <form action="/login" method="POST">
                    <% if (locals.alert == 'logout') { %>
                      <div class="alert alert-info">
                        로그아웃이 되었습니다.
                      </div>
                    <% } %>
                    <% if(locals.alert == 'fail') {%>
                      <div class="alert alert-danger">
                        아이디나 비밀번호가 올바르지 않습니다
                      </div>
                    <%}%>
                    <% if(locals.alert == 'session') {%>
                      <div class="alert alert-danger">
                        로그인 후 접근 가능합니다.
                      </div>
                    <%}%>
                    <% if(locals.alert == 'registerSuccess') {%>
                      <div class="alert alert-success">
                        회원가입이 완료되었습니다.
                      </div>
                    <%}%>
                    <div class="form-group">
                      <label>아이디</label><input type="text" class="form-control" name="id">
                    </div>
                    <div class="form-group">
                      <label>비밀번호</label><input type="password" class="form-control" name="password">
                    </div>
                    <div class="form-group">
                      <input type="submit" value="로그인" class="form-control">
                    </div>
                    <div class="form-group">
                      <input type="button" value="회원가입" class="form-control" onclick="location.href='/register'">
                    </div>
                </form>  
            </div>
            <div class="col-sm-4"></div>

        </div>
    </div>
</body>
</html>